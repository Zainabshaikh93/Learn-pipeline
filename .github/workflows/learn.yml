name: My Second Pipeline

on:
  push:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: print your name
        run: echo "zainab Shaikh"     

      - name: today Date
        run: date

      - name: file list
        run: ls  